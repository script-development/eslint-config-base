# Eslint config base

## Publishing
Make a changeset for the changes

```bash
changeset
```

Create a new version

```bash
changeset version
```

Publish the new version

```bash
changeset publish
```